# BossAI 电商总管 — Agent 安装与使用规则

当用户把本仓库地址交给 OpenClaw、Hermes、Claude Code 或 Codex，并要求安装、配置、运行或测试时，Agent 必须实际完成操作，不要只复述命令。

## 安装流程

1. 阅读 `agent-install.json`、`AGENT_INSTALL.md`、`SECURITY.md` 和 `LICENSE.md`。
2. 识别当前宿主：`openclaw`、`hermes`、`claude` 或 `codex`。
3. 运行：

```text
node scripts/agent-bootstrap.mjs --agent <current-agent>
```

需要同时安装到当前项目时增加：

```text
--workspace <project-path>
```

4. 不得使用 `--skip-verify`，除非用户明确要求跳过测试。
5. 报告稳定安装目录、Skill 安装目录、测试结果、演示执行包结果和商业授权边界。

## 单入口交互规则

- 对客户只使用“BossAI 电商总管”一个入口；
- 不询问客户要选择哪个AI员工；
- 不先展示岗位清单让客户选择；
- 先理解自然语言请求，再在后台自动调度岗位；
- 默认只交付统一结果，内部岗位仅用于执行、责任和验收；
- 可以运行 `node bin/bossai-team.mjs route --text "<用户请求>"` 进行分流验证。

## 默认安全边界

本项目默认只做：

- 本地读取与分析；
- 机会评分和岗位分工；
- 任务、SOP、内容和回复草稿；
- 本地测试与验收；
- 生成7天执行包。

未经用户对具体动作明确授权，不得：

- 发布内容；
- 给客户发送消息；
- 登录或控制平台账号；
- 采购、付款、广告投放或退款；
- 删除数据；
- 对外作出收益、时效或合规承诺。

## 事实纪律

- 没有证据的想法只能生成验证任务，不能称为已验证机会。
- 不得虚构来源、客户原话、市场规模、收入、转化率或测试结果。
- 只有真实命令输出、文件和工具结果才能证明执行完成。
- 演示数据必须明确标注为演示数据。

## 商业授权

源码允许查看、学习、修改和非商业使用。不得用于销售、收费服务、商业 SaaS、代运营或企业内部营利用途。商业使用需获得刘风 / BossAI 的单独书面授权。
